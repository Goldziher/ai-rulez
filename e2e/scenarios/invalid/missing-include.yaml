metadata:
  name: "Missing Include Project"
  version: "1.0.0"

includes:
  - "nonexistent.yaml"  # This file doesn't exist

outputs:
  - file: "output.md"

rules:
  - name: "Test Rule"
    content: "This should fail due to missing include"