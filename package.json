{
  "name": "ai-rulez",
  "version": "3.14.1",
  "description": "Directory-based AI governance for Claude, Cursor, Copilot, Windsurf and 14+ tools",
  "bin": {
    "ai-rulez": "./bin/ai-rulez"
  },
  "private": true,
  "scripts": {
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
