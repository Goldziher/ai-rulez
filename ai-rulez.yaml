metadata:
  name: "AI-Rulez Enforcement Test Simple"
  version: "1.0.0"
  description: "Simple test configuration for enforcement system"

# Sample rules without enforcement configuration
rules:
  - name: "no-console-log"
    content: "Avoid using console.log in production code. Use proper logging instead."

  - name: "proper-error-handling"
    content: "Always implement proper error handling with try-catch blocks or error return values."

# Sample sections
sections:
  - name: "coding-standards"
    content: |
      ## Coding Standards

      - Use meaningful variable and function names
      - Follow consistent indentation (2 spaces)
      - Keep functions small and focused
      - Write self-documenting code

outputs:
  - path: "CLAUDE.md"
    type: "rule"